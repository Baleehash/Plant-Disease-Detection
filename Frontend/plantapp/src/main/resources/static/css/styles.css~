/* General Reset */
body, h1, h2, p, ul, li, a {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    font-family: 'Poppins', Arial, sans-serif;
    line-height: 1.6;
    background-color: #f9f9f9;
    text-align: center;
}

/* Navbar */
.navbar {
    background-color: #f9f9f9;
    padding: 1rem 0;
    position: sticky;
    top: 0;
    z-index: 1000;
    box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1); /* Menambah bayangan lebih halus */
    transition: all 0.3s ease-in-out;
}

.navbar:hover {
    box-shadow: 0 6px 15px rgba(0, 0, 0, 0.2); /* Efek saat navbar ter-hover */
}

.navbar .container {
    display: flex;
    justify-content: space-between;
    align-items: center;
    max-width: 1200px;
    margin: auto;
    padding: 0 20px;
}

.navbar .logo {
    color: #1F4529;
    font-size: 1.8rem;
    font-weight: bold;
    text-decoration: none;
    letter-spacing: 1px;
}

.navbar .nav-links {
    list-style: none;
    display: flex;
    gap: 20px;
}

.navbar .nav-links a {
    color: #1F4529;
    text-decoration: none;
    font-size: 1rem;
    padding: 5px 10px;
    transition: all 0.3s;
    font-weight: 600; /* Menambah ketebalan font untuk tampilan lebih berani */
}

.navbar .nav-links a:hover {
    background-color: rgba(255, 255, 255, 0.2);
    border-radius: 5px;
    transform: translateY(-3px); /* Efek tombol melayang saat hover */
}

/* Hero Section */
.hero {
    background: linear-gradient(to right, #47663B, #1F4529);
    color: #fff;
    padding: 58px 20px;
    text-align: center;
}

.hero h1 {
    font-size: 3rem;
    margin-bottom: 20px;
}

.hero p {
    font-size: 1.2rem;
    max-width: 600px;
    margin: auto;
    margin-bottom: 30px;
}

.hero .btn {
    margin-top: 50px;
    margin-bottom: 30px;
    padding: 15px 30px;
    font-size: 1.2rem;
    border: 2px solid #fff;
    background-color: transparent;
    transition: all 0.3s;
}

.hero .btn:hover {
    background-color: #fff;
    color: #1F4529;
}

/* Sections */
section {
    padding: 50px 20px;
    margin-bottom: 20px;
}

section h2 {
    font-size: 2.5rem;
    color: #47663B;
    margin-bottom: 20px;
}

section p {
    margin-bottom: 30px;
    font-size: 0.9rem;
    color: #f9f9f9;
    overflow: hidden;
    border-right: 2px solid #f9f9f9;
    white-space: nowrap;
    width: 0;
    animation: typing 5s steps(40, end), blink 0.5s step-end infinite;
    animation-iteration-count: infinite;
}

/* Efek mengetik */
@keyframes typing {
    0% {
        width: 0;
    }
    50% {
        width: 100%; /* Muncul seluruh teks */
    }
    100% {
        width: 0; /* Reset ke awal */
    }
}

/* Button Styling */
section .btn {
    padding: 20px 30px;
    background-color: #47663B;
    color: #fff;
    border-radius: 30px;
    font-size: 1.1rem;
    transition: background-color 0.3s ease, transform 0.2s ease;
}

section .btn:hover {
    background-color: #264653;
    transform: translateY(-3px); /* Efek tombol melayang */
}

/* Features Section */
.features-section .card {
    border: 1px solid #ddd;
    border-radius: 10px;
    padding: 20px;
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    margin: 20px auto;
    max-width: 300px;
    text-align: center;
    transition: transform 0.3s, box-shadow 0.3s ease;
}

.features-section .card:hover {
    transform: scale(1.05);
    box-shadow: 0 6px 10px rgba(0, 0, 0, 0.2);
}

.features-section .card img {
    width: 100%;
    border-radius: 10px;
    margin-bottom: 15px;
}

.features-section .card h3 {
    font-size: 1.5rem;
    color: #264653;
    margin-bottom: 10px;
}

.features-section .card p {
    font-size: 1rem;
    color: #6b6b6b;
}

/* Footer */
.footer {
    background-color: #264653;
    color: #fff;
    text-align: center;
    padding: 20px 0;
    font-size: 0.9rem;
}

.footer a {
    color: #2a9d8f;
    text-decoration: none;
    font-weight: bold;
    transition: color 0.3s;
}

.footer a:hover {
    text-decoration: underline;
    color: #fff;
}

/* Responsive Design */
@media (max-width: 768px) {
    .navbar .nav-links {
        flex-direction: column;
        align-items: center;
    }

    .hero h1 {
        font-size: 2.5rem;
    }

    .hero p {
        font-size: 1rem;
    }

    .features-section .card {
        width: 100%;
    }
}